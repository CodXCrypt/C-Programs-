#include <stdio.h>

int main()
{
    long n,reverseNum;
    int d;
    printf("Enter the number : ");
    scanf("%ld",&n);
    while(n!=0)
    {
        d = n%10;
        reverseNum = reverseNum*10 + d;
        n = n/10;
    }
    printf("Reveresed Number is %ld",reverseNum);

    return 0;
}
